{
  "name": "xiyun",
  "displayName": "xiyun",
  "description": "禧云生态 VSCode 扩展插件",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.40.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.generator.list"
  ],
  "files": [
    "out",
    "src",
    "template"
  ],
  "main": "./out/index.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.generator.list",
        "title": "Generate XiYun List Template"
      },
      {
        "command": "extension.generator.create",
        "title": "Generate XiYun Create Template"
      },
      {
        "command": "extension.generator.detail",
        "title": "Generate XiYun Detail Template"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "extension.generator.list",
          "when": "explorerResourceIsFolder"
        },
        {
          "command": "extension.generator.create",
          "when": "explorerResourceIsFolder"
        },
        {
          "command": "extension.generator.detail",
          "when": "explorerResourceIsFolder"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "yarn run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "yarn run compile",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.11.7",
    "@types/vscode": "^1.40.0",
    "glob": "^7.1.5",
    "mocha": "^6.2.2",
    "tslint": "^5.20.0",
    "typescript": "^3.6.4",
    "vscode-test": "^1.2.2"
  },
  "dependencies": {
    "@types/fs-extra": "^8.0.1",
    "fs-extra": "^8.1.0"
  }
}
